default_app_config = 'channels_panel.apps.ChannelsDebugConfig'
GROUP_PREFIX = 'debug'
GROUP_NAME_GROUPS = '.'.join([GROUP_PREFIX, 'groups'])
GROUP_NAME_CHANNELS = '.'.join([GROUP_PREFIX, 'channels'])
_MARK = '_no_debug__'

